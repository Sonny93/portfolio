---
import BaseLayout from './base_layout.astro';
import type { OpenGraphType } from './base_layout.astro';
import Footer from '../components/navigation/footer.astro';
import Navbar from '../components/navigation/navbar.astro';

type Props = {
	title: string;
	description?: string;
	openGraphType?: OpenGraphType;
	image?: string;
	imageAlt?: string;
};

const { title, description, openGraphType, image, imageAlt } = Astro.props;
---

<BaseLayout
	title={title}
	description={description}
	openGraphType={openGraphType}
	image={image}
	imageAlt={imageAlt}
>
	<slot name="head" slot="head" />
	<div class="relative z-1 flex min-h-dvh flex-col">
		<div class="mx-auto flex w-full max-w-3xl flex-1 flex-col gap-6 p-4">
			<Navbar />
			<main
				id="main-content"
				tabindex="-1"
				class="flex flex-col gap-16 py-10 sm:gap-20 sm:py-16"
				data-page-transition
			>
				<slot />
			</main>
			<Footer />
		</div>
	</div>
</BaseLayout>
