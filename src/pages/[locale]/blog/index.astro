---
import { getPublishedPosts } from '../../../lib/posts';
import PageLayout from '../../../layouts/page_layout.astro';
import PostList from '../../../components/posts/post_list.astro';
import SectionTitle from '../../../components/section_title.astro';
import { LOCALES, resolveLocale, useTranslations } from '../../../i18n/ui';

export function getStaticPaths() {
	return LOCALES.map((locale) => ({ params: { locale } }));
}

const locale = resolveLocale(Astro.currentLocale);
const translate = useTranslations(locale);

const posts = await getPublishedPosts(locale);
---

<PageLayout
	title={translate('blog.pageTitle')}
	description={translate('blog.pageDescription')}
>
	<div>
		<SectionTitle title={translate('blog.pageTitle')} level="h1" />
		<PostList posts={posts} />
	</div>
</PageLayout>
