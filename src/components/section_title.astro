---
type Props = {
	title: string;
	level?: 'h1' | 'h2';
};

const { title, level = 'h2' } = Astro.props;
const HeadingTag = level;
---

<div class="relative mt-8 mb-8 flex w-full justify-center px-4 sm:mb-12">
	<span
		class="absolute -bottom-1/4 h-0.5 w-6 rounded-full bg-sky-400 shadow-[0_0_6px_1px_rgba(56,189,248,0.7)] sm:w-8"
		aria-hidden="true"></span>
	<HeadingTag
		class="text-center text-lg uppercase tracking-[0.18rem] text-gray-100 text-balance sm:text-xl sm:tracking-[0.25rem]"
	>
		{title}
	</HeadingTag>
</div>
