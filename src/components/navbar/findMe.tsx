import styled from '@emotion/styled';

const FindMe = styled.ul({
  listStyle: 'none',
  padding: 0,
  margin: 0,
  display: 'flex',
  gap: '0.75em',
  alignItems: 'center',
  justifyContent: 'center',
});

export default FindMe;
