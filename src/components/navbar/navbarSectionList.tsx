import styled from "@emotion/styled";

const NavbarSectionList = styled.ul({
  height: "fit-content",
  width: "100%",
  listStyle: "none",
  padding: 0,
  margin: "1em 0",
  flex: 1,
});

export default NavbarSectionList;
